<h1>Edit Cat</h1>

<form action="/cats/update/<%= @cat.id %>" method="post" enctype="multipart/form-data">
  <p>Edit the cat's name below.</p>
  <input type="text" name="name" placeholder="<%= @cat.name %>">

  
  <p>Add a different photo of this cat.</p>
  
  <% if !@cat.photo.nil? %>
    <img class="profile" src="<%= @cat.photo %>" />
  <% end %>
  
  <input type="file" name="photo">

  <p>When you're ready, click the button below to save the cat.</p>
  <input type="submit">
</form>

